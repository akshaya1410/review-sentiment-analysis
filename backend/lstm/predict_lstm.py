"""
Inference wrapper for the optional LSTM model.
"""

def predict_lstm_sentiment(review_text: str):
    """Placeholder wrapper for optional LSTM prediction."""
    return {
        "model": "LSTM Deep Learning Classifier",
        "notice": "The primary production server runs lightweight scikit-learn models for fast CPU execution."
    }
